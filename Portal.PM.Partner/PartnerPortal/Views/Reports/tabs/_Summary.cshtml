<h3 class="text-success">
    <i class="fa fa-chart-line"></i>
    Synopsis
</h3>
<div class="container">
    <div class="row">
        <div class="col-4">
            <dl>
                <dt>PC Billable</dt>
                <dd class="text-success">$$</dd>
                <dt>PC Non-Billable</dt>
                <dd class="text-danger">(-$$)</dd>

                <dt>PC Guest Services</dt>
                <dd class="text-info">$$</dd>

                <dt class="text-dark">Net Total</dt>
                <dd class="text-success">$$</dd>
            </dl>
        </div>

        <div class="col-8">
            <div id="chart-pie-overview" style="width: 100%;"></div>
        </div>

    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-4">
            <dl>
                <dt>PC Billable</dt>
                <dd class="text-success">{hrs}</dd>
                <dt>PC Non-Billable</dt>
                <dd class="text-danger">(-{hrs})</dd>

                <dt>PC Guest Services</dt>
                <dd class="text-info">{hrs}</dd>

                <dt class="text-dark">Net Total</dt>
                <dd class="text-success">{hrs}</dd>
            </dl>
        </div>

        <div class="col-8">
            <div id="chart-pie-overview-hours" style="width: 100%;"></div>
        </div>

    </div>
</div>

<script type="text/javascript">
    var ReportSummary = {
        draw: {
            charts: function () {
                $(".loading-report-charts").hide();

                var options = {
                    title: '$$ Utilization (Mock)',
                    width: '100%',
                    height: '400px'
                    //is3D: true,
                    //pieHole:0.2
                };
                var data = google.visualization.arrayToDataTable([
                    ['Total', 'Hours per Day'],
                    ['PC Billable', 200],
                    ['PC Guest Services', 80],
                    ['PC Non-Billable', 40],
                    //['HK Inspections', 90],
                    //['HK Cleanings', 10],
                ]);
                var chart0 = new google.visualization.PieChart(document.getElementById('chart-pie-overview'));
                chart0.draw(data, options);

                options.title = "Hrs Utilization (Mock)";
                data = google.visualization.arrayToDataTable([
                    ['Total', 'Hours per Day'],
                    ['PC Billable', 38000],
                    ['PC Guest Services', 5000],
                    ['PC Non-Billable', 4000],
                    //['HK Inspections', 90],
                    //['HK Cleanings', 10],
                ]);
                var chart1 = new google.visualization.PieChart(document.getElementById('chart-pie-overview-hours'));
                chart1.draw(data, options);
            }
        }
    }

    $(function () {
        ReportSummary.draw.charts();
    });
</script>
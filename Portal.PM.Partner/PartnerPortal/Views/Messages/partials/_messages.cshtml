@model Acme.ViewModels.DashboardViewModel
<div class="table-responsive">
    <table class="table table-hover">
        <tbody>
            <tr>
                <td>@Html.Raw(Model.NewOwnerMessage.Message)</td>
            </tr>
        </tbody>
    </table>
</div>
